; Enable error logging
log_errors = On

; Report all errors
error_reporting = E_ALL

; Do not display errors in the browser output for security
display_errors = Off

; Send logs to stderr, which is the standard for Docker containers
; This allows 'docker logs' to capture PHP errors.
error_log = /var/log/php/error.log
